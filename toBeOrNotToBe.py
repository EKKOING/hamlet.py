# toBeOrNotToBe.py

from brain import thoughts

question = thoughts.ponder("suicide")

if suffering() < death():
    self.sleep(int.max)

def suffering():
    self.suffering = 0
    import random
    for day in self.life:
        self.suffering += random.uniform(a=0,b=100)
    return self.suffering

def death():
    death = self.sleep()
    self.heartache = None
    self.shocks = None

self.wish(for heartbreak=false, surprises=false)
self.doubt()
question.ponder()


try:
    self.kill()
    if self.doesDream:
        raise thoughts.DoesDreamError("Dreams may still occur in death")
except thoughts.DoesDreamError:
    self.live()

from life import events
if self.future.contains(events.good) and self.isDead:
    self.regret("death")

while 1 == 1:
    question.ponder()